--create database radnik;

--use radnik;

--create table radnik(
--	sifra INT NOT NULL PRIMARY KEY,
--	ime NVARCHAR(50) NOT NULL,
--	prezime NVARCHAR(50) NOT NULL,
--	nadredeni INT NOT NULL FOREIGN KEY REFERENCES radnik(sifra)
--);